const dev = "localhost"
// const dev = "169.254.8.70"
export const baseURL = `http://${dev}:4000/api/`
export const wsURL = `ws://${dev}:4000`

export const headers = {
  Accept: "application/json",
  "Content-Type": "application/json"
}
